services:
  backend:
    build: ./Backend
    container_name: digi_prod
    ports:
      - "3000:3000"
    env_file:
      - ./Backend/.env
    volumes:
      - ./Backend:/app
      - /app/node_modules
    command: npm run dev